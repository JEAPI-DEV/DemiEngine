{
  "format_version": 1,
  "type": "object",
  "required": ["format_version", "id", "name"],
  "properties": {
    "format_version": { "type": "integer", "enum": [1] },
    "id": { "type": "string" },
    "name": { "type": "string" },
    "description": { "type": "string" },
    "maximum_stack": { "type": "integer", "minimum": 1 }
  }
}
